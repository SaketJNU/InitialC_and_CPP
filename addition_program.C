/* Program to add two numbers */

# include<stdio.h>
void main()
{ 
	int first_number, second_number, result;                             // All data types used in the program must be declared initail, whereas in C++, it can be declared before the first use of it
    // Declariation of the data type and vraiable name
    first_number = 4;      // Initailzation of the variable
    second_number = 5;   // Initailzation of the variable
    result = first_number + second_number;
    printf("required sum = %d", result);
}

